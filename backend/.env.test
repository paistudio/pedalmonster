# Fixed dummy secret used only to sign test JWTs locally/in CI — never a real Supabase value.
# See spec/support/jwt_helpers.rb.
SUPABASE_JWT_SECRET=test-only-dummy-secret-not-a-real-credential
CORS_ORIGINS=http://localhost:5173
